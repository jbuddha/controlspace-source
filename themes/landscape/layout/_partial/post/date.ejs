<a href="<%- url_for(post.path) %>" class="<%= class_name %>" style="text-transform: none;">
  <time datetime="<%= date_xml(post.date) %>" itemprop="datePublished"><%= date(post.date, date_format) %></time>
  <% if(post.author) { %>
      - <%- post.author %>
  <% } %>
</a>
